---
name: ai-podcast
description: 将文章、资料或录音制作成 AI 单人口播播客：可从获准的人工原声建立声线，写口播稿、用 OmniVoice 分段合成、检查并按授权分发。适用于任何说话人的新项目或已有稿件/声线的续做；不用于原声访谈剪辑。
---

# AI podcast

交付从材料、获准声线到可播放音频和可核对的平台结果。按现有进度进入相应阶段；已确认的声线、定稿和同一候选的发布授权直接复用。这里的“自动”指确定的合成、缓存、检查与打包可重跑；选题判断、事实核对、声音听审和外部发布结果仍需实际证据。

## 入口与项目记录

从当前任务确认材料、听众、语言、时长、说话人、声线使用范围和交付平台。未指定平台时先完成本地音频，不猜账号。单期工程分开保存来源、稿件、音频、发布候选和平台记录；项目已有目录约定时沿用。机器配置、账号归属、声音资产、文风材料和固定片头留在外部本地 profile，不写入共享 Skill。

每阶段记录 running / complete / failed / blocked 及实际产物路径。只有当前范围内阶段完成才宣称完成；“本地已生成”“待试听”“平台审核中”分别报告。恢复任务先查已有稿件、段落检查点与平台记录，不重复克隆、全量重生或重复发布。

## 1. 建立或选择声线

使用本 Skill 的脚本时读取 [声音与运行环境](references/voice-runtime.md)。当前实现用开源 `k2-fsa/OmniVoice` 做参考声音克隆；它不等于训练专属模型。说话人须有权使用该原声和数字声线；其他人的声线、公开发布和批量使用分别核对适用范围。

- 已有声线：读取该声线的当前 profile、运行要求、资产哈希和已确认语言。若专用声线 Skill 固定设备或参数，优先遵守；设备不可用时保留检查点，不擅自切换实现或用另一人声线。
- 新说话人：录制或取得获准人工原声 → 人工校对参考片段逐字稿 → 编码为候选 profile → 生成同文案试听 → 记录所选版本和语言。不同人使用不同 profile；合成成品不能充当下一轮克隆原声。
- 试听数量按任务确定；比较表达时覆盖叙述、转折、问题和数字。已批准声音不重新选型；新语言首次使用仍需试听，不能由中文批准推断英语质量。

## 2. 材料变成可讲述的稿件

读取 [内容与口播稿](references/content-script.md)。有定稿就保留，只做明确授权的朗读规范化；需要创作时先解析证据和论点，再写成单人讲述。

有原录音且需要内容母库时，可用环境中已有的转写流程建立来源映射；AI 单人口播不套用访谈的原音占比规则。用户提供个人文风材料或指定专用写作 Skill 时才使用，不把某个项目的私人语料当作通用风格。通用口播写法见参考文档。

产物为来源清单、事实/判断依据、原稿、实际合成稿，以及任务 JSON。风格、音色和事实核对是不同工作；不让旧笔记替用户作新判断，不编造第一人称经历。

## 3. 合成、核对与成品

先运行设备检查和短样测速。新 profile 可显式选择 CPU、MPS 或 CUDA；已有固定声线的设备/精度要求不能因故障静默改动。确认耗时可接受后按语义分段合成，保留段落缓存；失败只修复受影响段落，整集交付连续 WAV 与发布副本。可调用 `scripts/synthesize.py`，具体命令与配置见声音参考。

声音变化先比较原录音与已批准参考，不用加混响、变调或重度均衡掩盖远场、噪声或削波。检查漏句、重复、专名、数字、跨语言读音、连接处及实际时长；长稿用本地 ASR 辅助逐段核对，ASR 不能代替试听。保留未通过检查的状态，不能用“文件存在”代替验收。

按最终音频实际时间生成 OUTLINE、字幕或镜头映射，不能拿字数估计时码作为交付。默认不擅自加音乐、双主持人、品牌广告。发布编码、响度要求按本次平台核对，另导出副本且保留无损母版。

## 4. 登录、分发与结果核对

读取 [发布与分发](references/distribution.md)。先做好具体发布候选（音频、封面、标题、完整介绍、实际时间轴、用户指定链接/联系信息），生成哈希记录，再执行授权覆盖的平台与账号。登录态优先复用；确需验证码或扫码时只请用户完成那一步。

Firstory 等托管 RSS 与 Apple 等订阅目录分开处理；QQ/TME、喜马拉雅等分别确认账号和结果。不把上传成功写成已发布，不把 RSS 已添加写成中国区已收录。没有可用浏览器/连接器时完成发布包并说明卡点，不捏造操作结果。

## 完成与移植

展示可播放音频、稿件及实际发布链接/待处理事项。按项目要求归档，并校验副本哈希。分发不隐含后续定时监控授权。

复制整个本 Skill 文件夹即可移植流程；目标电脑另建 Python 环境、模型缓存、机器 runtime 和私有 voice profile。共享仓库不带原声、`.pt`、个人风格语料、账号凭据或本机配置。已执行的验证和边界见 [验证记录](references/validation.md)。Windows CPU 的短样测试不等于 Mac 实机或任意新说话人的验收。
